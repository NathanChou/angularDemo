import { InTheBoxDirective } from './in-the-box.directive';

describe('InTheBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new InTheBoxDirective();
    expect(directive).toBeTruthy();
  });
});
