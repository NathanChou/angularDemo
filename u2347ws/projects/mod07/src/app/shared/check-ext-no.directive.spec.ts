import { CheckExtNoDirective } from './check-ext-no.directive';

describe('CheckExtNoDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckExtNoDirective();
    expect(directive).toBeTruthy();
  });
});
